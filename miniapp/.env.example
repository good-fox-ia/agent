# База API. Лиши порожнім для '/api' на тому ж хості (рекомендовано в проді).
VITE_API_BASE_URL=

# 'true' — завжди mock-дані (зручно для розробки UI без бекенду).
VITE_USE_MOCK=

# Куди dev-сервер Vite проксує /api (Symfony). Не потрапляє в білд.
VITE_DEV_API_TARGET=http://localhost:8000
